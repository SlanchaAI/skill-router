# Opt-in TLS front door for Langfuse. Do not start this service until every demo credential is
# rotated and LANGFUSE_PUBLIC_URL matches the browser URL, as documented in docs/security.md.
https://{$LANGFUSE_HOST:localhost}:3443 {
	tls internal
	reverse_proxy langfuse-web:3000
}
