# Ingot MCP server — upper bounds sit one major above the versions the image is tested with,
# so a fresh `docker compose up --build` months from now can't silently jump an incompatible major.
fastmcp>=3,<4         # server (standalone FastMCP v3)
mcp>=1.2.0,<2         # client side, via langchain-mcp-adapters
fastembed>=0.4.0,<1
pyyaml>=6.0,<7
numpy>=1.26,<3

# LangGraph deep agent + MCP client
deepagents>=0.6,<0.7
langgraph>=0.2.60,<2
langchain-mcp-adapters>=0.1.0,<1
langchain-openai>=0.2.10,<2

# Tracing + skill optimization + approval UI
langfuse>=4.14,<5
skillopt==0.2.0    # body-pass optimizer: SkillOpt's held-out gate, patch-edit application, and
                   # update-mode helpers (microsoft/SkillOpt, MIT). Pinned exact — the loop matches
                   # this release's API; bump deliberately (see optimize/skillopt_bridge.py).
gepa==0.1.1        # still used by the routing/description pass only (optimize/routing.py)
litellm>=1.60,<2   # reflection-LM transport (openrouter/... model strings), shared by both passes
fastapi>=0.115,<1
uvicorn>=0.30,<1

# tests
pytest>=8.0,<10
