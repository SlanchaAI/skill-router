# MCP skill-router server
fastmcp>=3            # server (standalone FastMCP v3)
mcp>=1.2.0            # client side, via langchain-mcp-adapters
fastembed>=0.4.0
pyyaml>=6.0
numpy>=1.26

# LangGraph deep agent + MCP client
deepagents>=0.6,<0.7
langgraph>=0.2.60
langchain-mcp-adapters>=0.1.0
langchain-openai>=0.2.10

# Tracing + skill optimization (GEPA A/B) + approval UI
langfuse>=4.14,<5
gepa==0.1.1
litellm>=1.60      # gepa's reflection LM plumbing (openrouter/... model strings)
fastapi>=0.115
uvicorn>=0.30

# tests
pytest>=8.0
